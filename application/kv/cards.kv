<CustomerCard>:
    size_hint_y: None
    height: content.height

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 5
        rows: 1
        spacing: 10
        id: content

        MDTextField:
            width: 150
            size_hint_x: None
            text: root.customerCode
        MDTextField:
            text: root.customerName
        MDTextField:
            text: root.customerLocation
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.customerPhone
        MDBoxLayout:
            width: 50
            size_hint_x: None
            orientation: 'vertical'
            MDIconButton:
                icon: "content-save-edit"
                theme_icon_color: "Custom"
                icon_color: app.save_color
                on_press: app.update(root.parent.parent, root)

            MDIconButton:
                icon: "trash-can"
                theme_icon_color: "Custom"
                icon_color: app.error_color
                pos_hint: {"center_y": .5}
                on_press: app.delete(root.parent, root)


<WorkerCard>:
    size_hint_y: None
    height: content.height

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 7
        rows: 1
        spacing: 10
        id: content
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.workerCode
        MDTextField:
            text: root.workerFIO
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.professionCode
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.hireDate
        MDTextField:
            text: root.workerLocation
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.workerPhone
        MDBoxLayout:
            width: 50
            size_hint_x: None
            orientation: 'vertical'
            MDIconButton:
                icon: "content-save-edit"
                theme_icon_color: "Custom"
                icon_color: app.save_color
                on_press: app.update(root.parent.parent, root)

            MDIconButton:
                icon: "trash-can"
                theme_icon_color: "Custom"
                icon_color: app.error_color
                pos_hint: {"center_y": .5}
                on_press: app.delete(root.parent, root)


<WorkCard>:
    size_hint_y: None
    height: 100

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 4
        rows: 1
        spacing: 10
        MDTextField:
            text: root.workCode
        MDTextField:
            text: str(root.workPrice)
        MDTextField:
            text: str(root.workTime)
        MDBoxLayout:
            width: 50
            size_hint_x: None
            orientation: 'vertical'
            MDIconButton:
                icon: "content-save-edit"
                theme_icon_color: "Custom"
                icon_color: app.save_color
                on_press: app.update(root.parent.parent, root)

            MDIconButton:
                icon: "trash-can"
                theme_icon_color: "Custom"
                icon_color: app.error_color
                pos_hint: {"center_y": .5}
                on_press: app.delete(root.parent, root)


<SPTCard>:
    size_hint_y: None
    height: 100

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 5
        rows: 1
        spacing: 10
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.sptCode
        MDTextField:
            text: root.sptName
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.sptIzm
        MDTextField:
            width: 150
            size_hint_x: None
            text: str(root.sptPrice)
        MDBoxLayout:
            width: 50
            size_hint_x: None
            orientation: 'vertical'
            MDIconButton:
                icon: "content-save-edit"
                theme_icon_color: "Custom"
                icon_color: app.save_color
                on_press: app.update(root.parent.parent, root)

            MDIconButton:
                icon: "trash-can"
                theme_icon_color: "Custom"
                icon_color: app.error_color
                pos_hint: {"center_y": .5}
                on_press: app.delete(root.parent, root)


<SpecificationCard>:
    size_hint_y: None
    height: 100
    adaptive_height: True

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 7
        rows: 1
        spacing: 10
        adaptive_height: True
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.specificationCode
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.contractCode
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.customerCode
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.sptCode
        MDBoxLayout:
            orientation: 'vertical'
            id: panel_layout
            adaptive_height: True
        MDTextField:
            width: 150
            size_hint_x: None
            text: str(root.cost)
        MDBoxLayout:
            width: 50
            size_hint_x: None
            orientation: 'vertical'
            MDIconButton:
                icon: "content-save-edit"
                theme_icon_color: "Custom"
                icon_color: app.save_color
                on_press: app.update(root.parent.parent, root)

            MDIconButton:
                icon: "trash-can"
                theme_icon_color: "Custom"
                icon_color: app.error_color
                pos_hint: {"center_y": .5}
                on_press: app.delete(root.parent, root)


<TaskCard>:
    size_hint_y: None
    height: 100
    adaptive_height: True

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 7
        rows: 1
        spacing: 10
        adaptive_height: True
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.taskCode
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.contractCode
        MDTextField:
            width: 150
            size_hint_x: None
            text: root.taskDate
        MDBoxLayout:
            orientation: 'vertical'
            id: panel_layout
            adaptive_height: True
        MDTextField:
            width: 150
            size_hint_x: None
            text: str(root.taskCost)
        MDBoxLayout:
            width: 50
            size_hint_x: None
            orientation: 'vertical'
            MDIconButton:
                icon: "content-save-edit"
                theme_icon_color: "Custom"
                icon_color: app.save_color
                on_press: app.update(root.parent.parent, root)

            MDIconButton:
                icon: "trash-can"
                theme_icon_color: "Custom"
                icon_color: app.error_color
                pos_hint: {"center_y": .5}
                on_press: app.delete(root.parent, root)


<CustomerReportCard>:
    size_hint_y: None
    height: 100
    adaptive_height: True

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 3
        rows: 1
        spacing: 10
        adaptive_height: True
        MDLabel:
            text: root.customerCode
        MDLabel:
            text: str(root.contractNumber)
        MDLabel:
            text: str(root.contractPrice)


<WorkerReportCard>:
    size_hint_y: None
    height: 100
    adaptive_height: True

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 4
        rows: 1
        spacing: 10
        adaptive_height: True
        MDLabel:
            text: root.workerCode
        MDLabel:
            text: root.workerFIO
        MDLabel:
            text: str(root.taskNumber)
        MDLabel:
            text: str(root.workPrice)


<AllReportCard>:
    size_hint_y: None
    height: 100
    adaptive_height: True

    MDGridLayout:
        height: 100
        size_hint_y: None
        cols: 4
        rows: 1
        spacing: 10
        adaptive_height: True
        MDLabel:
            text: str(root.allContractNumber)
        MDLabel:
            text: str(root.completeContractNumber)
        MDLabel:
            text: str(root.sptNumber)
        MDLabel:
            text: str(root.workPrice)
